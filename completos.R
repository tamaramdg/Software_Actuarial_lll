completos<- function(specdata, id=1:332){
  suma=0
  for (i in id){
    for (1 in row){
      if is.na(fila)
    suma=suma
    else if 
    suma=suma+1
    }
    data.frame(id, suma, rownames="ID", "nobs")
  }
}
  